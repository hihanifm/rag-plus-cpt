"""ragcpt — carrier-aware Qwen3-14B SFT pipeline engine.

The engine is a plain library. Frontends (run_mvp.py now; CLI / Streamlit / FastAPI later) are thin
wrappers that call these functions. No business logic lives in any frontend.
"""

__version__ = "0.1.0"
