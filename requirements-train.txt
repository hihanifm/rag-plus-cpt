# Training + serving deps — install ONLY on the GPU host (Lambda H100 / DGX), not the Mac.
#   pip install -r requirements-train.txt
# LLaMA-Factory pulls torch/transformers/peft/accelerate/datasets as deps.
llamafactory>=0.9.0
vllm>=0.6.0
# torch is pulled by the above; if you need a specific CUDA build, install torch first.
